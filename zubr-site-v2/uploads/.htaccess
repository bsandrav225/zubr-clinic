Options -Indexes

# Запрещаем выполнение PHP среди загруженных файлов (защита при подмене расширения)
<FilesMatch "\.(php|phtml|php\d)$">
    <IfModule mod_authz_core.c>
        Require all denied
    </IfModule>
    <IfModule !mod_authz_core.c>
        Deny from all
    </IfModule>
</FilesMatch>
